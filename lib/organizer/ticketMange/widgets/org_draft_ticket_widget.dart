import 'package:flutter/widgets.dart';

class OrgDraftTicketWidget extends StatelessWidget {
  const OrgDraftTicketWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
